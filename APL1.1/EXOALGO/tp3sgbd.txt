EXO1


select count(ALL NUMVIN), REGION
from vin
group by REGION;


EXO2

SELECT COUNT(ALL CRU),NUMVITIC,NOM
FROM VIN NATURAL JOIN VITICULTEUR
GROUP BY NUMVITIC,NOM;

EXO3

SELECT AVG(ALL QTTE),NUMBUVEUR,NOM
FROM COMMANDE NATURAL JOIN BUVEUR
WHERE VILLE = 'PARIS'
GROUP BY NUMBUVEUR,NOM;